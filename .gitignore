# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/

# Coverage and testing
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
.ruff_cache/

# Pipeline output (JSONL ground truth + SQLite query layer). Scoped to the repo root
# and the documented default names so fixture payloads under tests/ stay tracked.
/pdx1_signals.jsonl
/pdx1.db
/*.jsonl
/*.db

# Last-good payload cache for live adapters (PDX1_CACHE_DIR). Fetched bodies, not
# source — rebuilt on the next successful cycle.
/cache/
